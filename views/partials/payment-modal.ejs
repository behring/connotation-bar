<!-- Modal -->
<div class="modal fade" id="payment-modal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title" id="myModalLabel">温馨提示</h4>
            </div>
            <div class="modal-body">
                <% if(user.get('funnyCoin') < 100) { %>
                你当前剩余趣币：<strong><%= user.get('funnyCoin') || 0%></strong> ，无法查看当前内容。<a href="">去赞助一下吧</a>
                <% } else { %>
                你当前剩余趣币：<strong><%= user.get('funnyCoin') || 0%></strong> ，是否愿意花费100趣币查看当前内容？
                <% } %>

            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default" onclick="javascript:history.back(-1);">算了吧</button>
                <a href="<%= user.get('funnyCoin') < 100 ? '/mine?menu=个人信息' : '/payment?pictureId='+ picture.get('objectId') %>">
                    <button type="button" class="btn btn-primary">
                        <%= user.get('funnyCoin') < 100 ? '去赞助' : '我要看' %></button>
                </a>
            </div>
        </div>
    </div>
</div>